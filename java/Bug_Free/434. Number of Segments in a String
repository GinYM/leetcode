class Solution {
    public int countSegments(String s) {
        int count = 0;
        for(String str : s.split(" ")){
            count += str.length() == 0 ? 0 : 1;
        }
        return count;
    }
}
